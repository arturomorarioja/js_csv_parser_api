services:
  node-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      BASE_DIR: /app
    volumes:
      - .:/app:ro
    networks:
      - app_network

networks:
  app_network: